{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Run FastAPI App",
        "type": "python",
        "request": "launch",
        "module": "backend.main",
        "env": {
          "PYTHONPATH": "${workspaceFolder}"
        },
        "console": "integratedTerminal"
      }
    ]
  }
  